name: Sync sam3 → HF
on:
  push:
    branches: [main]
    paths: ['sam3/**', '.github/workflows/sync-sam3.yml']

jobs:
  sync:
    uses: ./.github/workflows/_sync-folder.yml
    with:
      folder: sam3
    secrets:
      HF_TOKEN: ${{ secrets.HF_TOKEN }}
